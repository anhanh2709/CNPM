let initialState = {
    theaters: [],
};

const TheaterReducer = (state = initialState, action) => {
    switch (action.type) {
        default: return state;
    }
};

export default TheaterReducer;